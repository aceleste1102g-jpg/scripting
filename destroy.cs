using UnityEngine;

public class destroyed : MonoBehaviour
{
    // destroy objet
    void Start()
    {
        Destroy(gameObject);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
